/**
 * An Entity's Partition Key. A Partition key is used to partition entities into a single canister or group of canisters with that same partition key
 */
export type PartitionKey = string;
/**
 * An Entity's Sort Key. A Sort key is used to sort entities within a canister or group of canisters with a specific partition key
 */
export type SortKey = string;
